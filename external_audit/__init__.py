# External robustness audit for the frozen Dataset V4 deployment model.
#
# This package is deliberately separate from the official Dataset V4 benchmark.
# It must never be used to tune or replace the locked final-test result.
